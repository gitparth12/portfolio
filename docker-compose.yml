services:
  nextjs-site:
    build: .
    container_name: nextjs-site
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    command: npm start
